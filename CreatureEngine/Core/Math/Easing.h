#include"Core/Common.h"

 

namespace Math 
{
    /* Ease Linear(inputvalue, outputmin,outputmax,inputmax) */
    CREATURE_API float easeLinear(float _input, float _outputmin, float _outputmax, float _inputmax);



    /* Ease In Quad(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInQuad(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* Ease Out Quad(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutQuad(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* Ease In Out Quad(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutQuad(float _input, float _outputmin, float _outputmax, float _inputmax);
    


    /* EaseInCubic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInCubic(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseOutCubic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutCubic(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseInOutCubic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutCubic(float _input, float _outputmin, float _outputmax, float _inputmax);



    /* EaseInQuart(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInQuart(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseOutQuart(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutQuart(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseInOutQuart(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutQuart(float _input, float _outputmin, float _outputmax, float _inputmax);



    /* EaseInQuint(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInQuint(float _input, float _outputmin, float _outputmax, float _inputmax);
    
    /* EaseOutQuint(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutQuint(float _input, float _outputmin, float _outputmax, float _inputmax);
      
    /* EaseInOutQuint(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutQuint(float _input, float _outputmin, float _outputmax, float _inputmax);
      


    /* EaseInSine(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInSine(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseOutSine(inputvalue,outputmin,outputmax,inputmax)*/
    CREATURE_API float easeOutSine(float _input, float _outputmin, float _outputmax, float _inputmax);
    
    /* EaseInOutSine(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutSine(float _input, float _outputmin, float _outputmax, float _inputmax);
    


    /* EaseInCirc(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInCirc(float _input, float _outputmin, float _outputmax, float _inputmax);
    
    /* EaseInOutCirc(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutCirc(float _input, float _outputmin, float _outputmax, float _inputmax);



    /* EaseInExpo(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInExpo(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseOutExpo(inputvalue,outputmin,outputmax,inputmax); */
    CREATURE_API float easeOutExpo(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseInOutExpo(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutExpo(float _input, float _outputmin, float _outputmax, float _inputmax);



    /* EaseInElastic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInElastic(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseOutElastic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutElastic(float _input, float _outputmin, float _outputmax, float _inputmax);
    
    /* EaseInOutElastic(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutElastic(float _input, float _outputmin, float _outputmax, float _inputmax);
    
        
    
    /* EaseInBack(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInBack(float _input, float _outputmin, float _outputmax, float _inputmax);
    
    /* EaseOutBack(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeOutBack(float _input, float _outputmin, float _outputmax, float _inputmax);

    /* EaseInOutBack(inputvalue,outputmin,outputmax,inputmax) */
    CREATURE_API float easeInOutBack(float _input, float _outputmin, float _outputmax, float _inputmax);
      


    /* EaseInBounce(inputvalue,outputmin,outputmax,inputmax)*/
    CREATURE_API float easeInBounce(float _input, float _outputmin, float _outputmax, float _inputmax);
     
    /* EaseOutBounce(inputvalue,outputmin,outputmax,inputmax)*/
    CREATURE_API float easeOutBounce(float _input, float _outputmin, float _outputmax, float _inputmax);
  
    /* EaseInOutBounce(inputvalue,outputmin,outputmax,inputmax)  */
    CREATURE_API float easeInOutBounce(float _input, float _outputmin, float _outputmax, float _inputmax);
    
 
}// NS Math